# MCP Server Environment Variables
# Copy to .env and fill in your values (or export directly)

# Server configuration
PORT=8000
NODE_ENV=development

# Excel Engine URL (local or Render internal URL)
EXCEL_ENGINE_BASE_URL=http://localhost:5001

# Widget public URL (Vercel deployment URL)
# Used for CORS and CSP configuration
WIDGET_PUBLIC_URL=http://localhost:3001

# Backblaze B2 download URL (for CSP headers)
# Set to match Excel Engine's B2_DOWNLOAD_URL or leave empty for defaults
# B2_DOWNLOAD_URL=https://f005.backblazeb2.com

# Contracts directory (optional, auto-detected)
# CONTRACTS_DIR=/path/to/contracts
